from .layers import *
from .activations import *
from .losses import *
from .optimizers import *
from .utils import *


# Basic neural network module

# This module provides the core neural network components for the AnyPlay
# It includes layers, activations, losses, optimizers, and utilities

# Example usage:
# nn = NeuralNetwork()
# nn.add_layer(Dense(10, 5))
# nn.add_layer(Activation('relu'))
# nn.compile(Adam(), MSE())
# nn.fit(train_data, train_labels)

# Note: This is a basic implementation. The actual implementation
# will be more complex and will include the actual neural network
# components that are needed for the AnyPlay.
